// MonyaCode keymap
//
// For information on binding keys, see the MonyaCode
// documentation: monyacode://docs/key-bindings
//
// To see the default key bindings run `monyacode: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    }
  }
]
